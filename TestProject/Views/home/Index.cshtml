@model TestProject.Models.StudentViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<link href="~/Scripts/Draggable/app.css" rel="stylesheet" />
<div class="container">
    <div class="row">
        <div class="col-md-2">Student Name</div>
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.StudnetModel.StudentName, htmlAttributes: new { @class = "form-control" })
        </div>
        <div class="col-md-2">Course</div>
        <div class="col-md-10">
            @Html.DropDownList("CourseDrodown", null, htmlAttributes: new { @class = "form-control" })
        </div>
    </div>
    @*<div class="row">


            <div id="AvailableCourse" class="row col-md-6" style="">
                @{
                    <p>Available Course</p>
                    <ul>
                        @foreach (var item in Model.StudentCoures)
                        {
                            <li class="col-xs-12 draggableobject ui-draggable ui-draggable-handle" style="background:#00f">@item.CourseName</li>
                        }
                    </ul>
                }

            </div>

            <div id="Selected Course" class="droppable col-md-6" style="height:100px;border:0px solid #000;z-index:-444">

            </div>

        </div>*@




    <div id="main_window">
        <div id="left_pane">
            <h4>Draggables</h4>
                <div id="available_metrics_filter">
                    <ul id="available_metrics_list">
                        
                    </ul>
                </div>
        </div>
        <div id="right_pane">
            <h4>Droppable</h4>
                <div id="selected_metrics">
                    <ul id="selected_metrics_list">
                        <li id="next_metric">Next Draggable</li>
                    </ul>
                </div>
                <div id="remove_metric">
                    Remove Draggable
                </div>
        </div>
    </div>

</div>
<script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js'></script>
<script src='https://ajax.googleapis.com/ajax/libs/jqueryui/1.11.2/jquery-ui.min.js'></script>

<script src="~/Scripts/Draggable/app.js"></script>
<script>
    $(document).ready(function () {
    
    });

    //$(document).ready(function () {
    //    $(".draggableobject").draggable();
    //    $(".droppable").droppable({
    //        drop: function (event, ui) {
    //            //Get the position before changing the DOM
    //            var p1 = ui.draggable.parent().offset();
    //            //Move the element to the new parent
    //            $(this).append(ui.draggable);
    //            //Get the postion after changing the DOM
    //            var p2 = ui.draggable.parent().offset();
    //            //Set the position relative to the change
    //            ui.draggable.css({
    //                top: parseInt(ui.draggable.css('top')) + (p1.top - p2.top),
    //                left: parseInt(ui.draggable.css('left')) + (p1.left - p2.left)
    //            });
    //        }
    //    });
    //});
</script>